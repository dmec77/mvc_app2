// const express = require('express'),
//   router = express.Router(),
//
// const controller = require('./controller');
//
// // this is telling the app that when a request is made on ANY page ("/")then access this named Fx we created in the users/controller.js (inside the TOP Controllers Folder)
//
// router.route('/')
//     .post(controller.create);
//
// // this is telling the app that when a request is made on ANY page ("/sign-up")then access this named Fx we created in the users/controller.js (inside the TOP Controllers Folder)
//
// router.route('/sign_up')
//     .get(controller.new);
//
//     // this is telling the app that when a request is made on ANY page ("/login")then access this named Fx we created in the users/controller.js (inside the TOP Controllers Folder)
//
// router.route('/login')
//     .get(controller.login)
//     .post(controller.process_login);
//
//
// module.exports = router;
